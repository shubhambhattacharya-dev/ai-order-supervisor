[project]
name = "backend"
version = "0.1.0"
description = "AI Order Supervisor API"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "fastapi[standard]>=0.141.1",
    "temporalio>=1.32.0",
]

