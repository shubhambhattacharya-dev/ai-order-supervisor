[project]
name = "worker"
version = "0.1.0"
description = "Temporal worker for the AI Order Supervisor"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "temporalio==1.32.0",
]